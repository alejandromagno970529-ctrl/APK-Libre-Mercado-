name: libre_mercado_final__app
description: "Aplicación de mercado libre hiperlocal"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  
  # Supabase - Versiones actualizadas
  supabase_flutter: ^2.10.3
  supabase: ^2.10.0
  
  # State Management
  provider: ^6.1.1
  
  # Image Handling
  image_picker: ^1.0.4
  
  # Location - Versiones actualizadas
  geolocator: ^14.0.2
  geocoding: ^4.0.0
  
  # HTTP Client
  http: ^1.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true